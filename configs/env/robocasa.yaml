# Universal environment configuration
# This is the ONLY env config file
# Specify the task name via command line: env.name=CloseDrawer

# Environment name (must be specified)
name: ???

# Path to complete robomimic-style JSON config (auto-constructed from name)
# All environment configuration (env_meta_update_dict, observation, etc.) is in this JSON
json_config_path: ${paths.root}/configs/env/configs/${env.name}.json

# Runtime environment settings (override JSON config)
# These settings take precedence over values in the JSON config
render: false  # Whether to render on-screen (for visualization)
layout_and_style_ids: [[0, 1]]  # Scene layout and style combinations

# Data collection settings (shared across all tasks)
data_collection:
  # Scene depth cameras to use for N2M point cloud capture (following reference implementation)
  # These are fixed scene cameras (depth_camera1-5), not robot-mounted cameras
  depth_cameras:
    - depth_camera1
    - depth_camera2
    - depth_camera3
    - depth_camera4
    - depth_camera5
  
  # Initial pose sampling range for randomization. This is for n2m data collection.
  # Following reference implementation: x_half_range=0.2, y_half_range=0.2, theta_half_range_deg=15
  # theta: 15 degrees = 0.262 radians
  initial_pose_randomization_range:
    x: [-0.4, 0.4]
    y: [-0.4, 0.4]
    theta: [-0.262, 0.262]

  # debug
  # initial_pose_randomization_range:
  #   x: [-0.0, 0.0]
  #   y: [-0.0, 0.0]
  #   theta: [-0.0, 0.0]
