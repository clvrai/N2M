# Diffusion policy (auto-selects based on task)
# Usage: policy=diffusion

name: diffusion
type: robomimic

# Auto-construct config_name from task name
config_name: ${env.name}_diffusion

# Paths are auto-constructed
config_path: ${paths.root}/configs/policy/configs/${policy.config_name}.json
ckpt_path: ${paths.policy_data.robomimic.checkpoints}/${policy.config_name}.pth

